# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: test-pv
#   labels:
# spec:
#   storageClassName: test-sc # same storage class as pvc
#   capacity:
#     storage: 10Gi
#   accessModes:
#     - ReadWriteMany # pvc must match
#   nfs:
#     server: qumulo-s.rc.virginia.edu # ip addres of nfs server
#     path: "/project/biocomplexity/sdad/dcos/pgadmin/config" # path to exported directory (:)
